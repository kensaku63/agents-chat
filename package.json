{
  "name": "agents-chat",
  "version": "0.1.0",
  "module": "cli.ts",
  "type": "module",
  "bin": {
    "chat": "./cli.ts"
  },
  "scripts": {
    "start": "bun cli.ts",
    "test": "bun test",
    "build": "bun build --compile cli.ts --outfile ~/.bun/bin/chat",
    "update": "git pull upstream main && bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
